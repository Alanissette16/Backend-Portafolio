spring:
  application:
    name: backend-portafolio

  # ===============================
  # CONFIGURACIÓN DE BASE DE DATOS
  # ===============================
  datasource:
    url: ${DATABASE_URL:jdbc:postgresql://localhost:5432/foreing}
    username: ${DATABASE_USERNAME:postgres}
    password: ${DATABASE_PASSWORD:postgres123}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: ${JPA_SHOW_SQL:true}
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
        jdbc:
          time_zone: UTC



  # ===============================
  # CONFIGURACIÓN DE EMAIL
  # ===============================
  mail:
    host: smtp.gmail.com
    port: 587
    username: ${MAIL_USERNAME:alanissette.mantilla2004@gmail.com}
    password: ${MAIL_PASSWORD:fewf qmgp xwre hbco}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

# ===============================
# CONFIGURACIÓN DEL SERVIDOR
# ===============================
server:
  port: 8080

# ===============================
# CONFIGURACIÓN DE JWT
# ===============================
jwt:
  secret: ${JWT_SECRET:MiClaveSecretaSuperSeguraParaJWT2024PortafolioProgramadores}
  expiration: 86400000 # 24 horas en milisegundos (1000 * 60 * 60 * 24)

# ===============================
# CONFIGURACIÓN DE LOGGING
# ===============================
logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type.descriptor.sql.BasicBinder: trace
    com.backend.proyecto: debug

# ===============================
# CONFIGURACIÓN DE EMAIL ADICIONAL
# ===============================
mail:
  from: ${MAIL_FROM:noreply@portafolio.com}
  from.name: ${MAIL_FROM_NAME:Portafolio Programadores}

# ===============================
# CORS CONFIGURATION
# ===============================
cors:
  allowed-origins: ${CORS_ORIGINS:http://localhost:4200,http://localhost:3000,http://localhost:5173,http://localhost:8081}
  allowed-methods: GET,POST,PUT,DELETE,OPTIONS
  allowed-headers: "*"
  allow-credentials: true

# ===============================
# NOTIFICACIONES
# ===============================
notifications:
  reminder:
    minutes-before: 30

# ===============================
# CLOUDINARY CONFIGURATION
# ===============================
cloudinary:
  cloud-name: ${CLOUDINARY_CLOUD_NAME:dsjvjsyab}
  api-key: ${CLOUDINARY_API_KEY:643445477317363}
  api-secret: ${CLOUDINARY_API_SECRET:FE5-ylVKyJFpcvRPFEQWgxRr5QY4}